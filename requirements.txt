# Install a CUDA-matched torch/torchvision build FIRST from
# https://pytorch.org/get-started/locally/ (pick your CUDA version),
# then install the rest of these with pip.
torch>=2.1.0
torchvision>=0.16.0
opencv-python>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
tqdm>=4.66.0
Pillow>=10.0.0